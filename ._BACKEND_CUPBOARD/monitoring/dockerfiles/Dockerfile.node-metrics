FROM node:18-alpine

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm ci --only=production

COPY . .

ENV PORT=8080
ENV METRICS_PORT=9100
EXPOSE 8080 9100

CMD ["sh", "-c", "node server/index.js & node monitoring/server/metrics.js"]
.