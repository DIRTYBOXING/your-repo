diff --git a/.github/workflows/payments-ci.yml b/.github/workflows/payments-ci.yml
index 72898d47..d80aa43f 100644
--- a/.github/workflows/payments-ci.yml
+++ b/.github/workflows/payments-ci.yml
@@ -26,9 +26,41 @@ concurrency:
   group: payments-exp-${{ github.ref }}
   cancel-in-progress: true
 
+permissions:
+  contents: read
+  security-events: write
+
 jobs:
+  secrets-scan:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+        with:
+          fetch-depth: 0
+      - name: Run gitleaks
+        uses: gitleaks/gitleaks-action@v2
+        env:
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+
+  dependency-scan:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+      - uses: actions/setup-node@v4
+        with:
+          node-version: "20"
+          cache: "npm"
+          cache-dependency-path: backend/payments/package-lock.json
+      - name: Run npm audit (production deps)
+        run: |
+          cd backend/payments
+          npm ci
+          npm audit --omit=dev --audit-level=high
+
   migration-smoke:
+    needs: [secrets-scan, dependency-scan]
     runs-on: ubuntu-latest
+    timeout-minutes: 20
     services:
       postgres:
         image: postgres:15
@@ -52,18 +84,51 @@ jobs:
         run: sudo apt-get update && sudo apt-get install -y postgresql-client
       - name: Migration up
         run: |
-          psql "$PG_CONN" -f migrations/20260702_create_payments_and_webhooks.sql
-          psql "$PG_CONN" -f migrations/20260702_create_rights_and_takedown.sql
-          psql "$PG_CONN" -f migrations/20260702_create_experiments_and_assignments.sql
+          set -euo pipefail
+          psql "$PG_CONN" -f migrations/20260702_create_payments_and_webhooks.sql | tee migration_up_payments.log
+          psql "$PG_CONN" -f migrations/20260702_create_rights_and_takedown.sql | tee migration_up_rights.log
+          psql "$PG_CONN" -f migrations/20260702_create_experiments_and_assignments.sql | tee migration_up_experiments.log
       - name: Migration down
         run: |
-          psql "$PG_CONN" -f migrations/20260702_drop_experiments_and_assignments.sql
-          psql "$PG_CONN" -f migrations/20260702_drop_rights_and_takedown.sql
-          psql "$PG_CONN" -f migrations/20260702_drop_payments_and_webhooks.sql
+          set -euo pipefail
+          psql "$PG_CONN" -f migrations/20260702_drop_experiments_and_assignments.sql | tee migration_down_experiments.log
+          psql "$PG_CONN" -f migrations/20260702_drop_rights_and_takedown.sql | tee migration_down_rights.log
+          psql "$PG_CONN" -f migrations/20260702_drop_payments_and_webhooks.sql | tee migration_down_payments.log
+      - name: Verify rollback reached clean state
+        run: |
+          set -euo pipefail
+          psql "$PG_CONN" -tAc "
+            SELECT COUNT(*)
+            FROM information_schema.tables
+            WHERE table_schema='public'
+              AND table_name IN (
+                'webhook_events',
+                'provider_transactions',
+                'orders',
+                'ppv_entitlements',
+                'ledger_entries',
+                'payout_batches',
+                'payout_attempts',
+                'content_rights',
+                'geo_policy',
+                'takedown_requests',
+                'experiments',
+                'assignments',
+                'exposures',
+                'experiment_audit'
+              )
+          " | grep -qx '0'
+      - name: Upload migration logs
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: migration-smoke-logs
+          path: "*_*.log"
 
   payments-node-test:
     needs: migration-smoke
     runs-on: ubuntu-latest
+    timeout-minutes: 20
     services:
       postgres:
         image: postgres:15
@@ -94,13 +159,21 @@ jobs:
         run: psql "$PG_CONN" -f migrations/20260702_create_payments_and_webhooks.sql
       - name: Run payments tests
         run: |
+          set -euo pipefail
           cd backend/payments
           npm ci
-          npm test
+          npm test | tee ../../payments-tests.log
+      - name: Upload payments test logs
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: payments-test-logs
+          path: payments-tests.log
 
   dart-rights-experiments-test:
     needs: migration-smoke
     runs-on: ubuntu-latest
+    timeout-minutes: 20
     steps:
       - uses: actions/checkout@v4
       - uses: subosito/flutter-action@v2
@@ -111,8 +184,20 @@ jobs:
       - name: Flutter pub get
         run: flutter pub get
       - name: Analyze scoped backend modules
-        run: flutter analyze backend/rights backend/experiments test/backend/rights test/experiments
+        run: |
+          set -euo pipefail
+          flutter analyze backend/rights backend/experiments test/backend/rights test/experiments | tee dart-analyze.log
       - name: Run rights and experiments tests
         run: |
-          flutter test test/backend/rights
-          flutter test test/experiments/assignment_test.dart
+          set -euo pipefail
+          flutter test test/backend/rights | tee dart-rights-tests.log
+          flutter test test/experiments/assignment_test.dart | tee dart-experiments-tests.log
+      - name: Upload dart logs
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: dart-rights-experiments-logs
+          path: |
+            dart-analyze.log
+            dart-rights-tests.log
+            dart-experiments-tests.log
diff --git a/docs/runbooks/staging_hardening_runbook.md b/docs/runbooks/staging_hardening_runbook.md
new file mode 100644
index 00000000..17915615
--- /dev/null
+++ b/docs/runbooks/staging_hardening_runbook.md
@@ -0,0 +1,121 @@
+# Staging Hardening Runbook
+
+## Scope
+This runbook validates the payments, rights, and experiments paths for a production-hardening snapshot.
+
+## Preconditions
+- Docker installed (for local Postgres bootstrap)
+- Node.js 20+
+- Flutter stable
+- Dart SDK available through Flutter
+- psql available for direct migration execution
+
+## 1) Create hardening branch snapshot
+
+PowerShell:
+
+$branch = "hardening/release-$(Get-Date -Format yyyy-MM-dd)"
+git checkout -b $branch
+git add -A
+git commit -m "chore(release): snapshot for hardening"
+git push origin HEAD
+
+## 2) Bootstrap local Postgres (optional)
+
+PowerShell:
+
+./scripts/test/bootstrap_pg.ps1
+
+This sets PG_CONN in the current shell output. If needed, export it manually:
+
+$env:PG_CONN = "postgres://dfc:dfc@localhost:5432/dfc_test"
+
+Teardown:
+
+./scripts/test/teardown_pg.ps1
+
+## 3) Apply migrations (staging or local)
+
+PowerShell:
+
+psql "$env:PG_CONN" -f migrations/20260702_create_payments_and_webhooks.sql
+psql "$env:PG_CONN" -f migrations/20260702_create_rights_and_takedown.sql
+psql "$env:PG_CONN" -f migrations/20260702_create_experiments_and_assignments.sql
+
+Rollback smoke:
+
+psql "$env:PG_CONN" -f migrations/20260702_drop_experiments_and_assignments.sql
+psql "$env:PG_CONN" -f migrations/20260702_drop_rights_and_takedown.sql
+psql "$env:PG_CONN" -f migrations/20260702_drop_payments_and_webhooks.sql
+
+## 4) Start payments reference server
+
+PowerShell:
+
+cd backend/payments
+npm ci
+$env:PG_CONN = "postgres://dfc:dfc@localhost:5432/dfc_test"
+node index.js
+
+Expected:
+- log contains server listening message
+- no immediate DB connection failures during webhook/verify-session calls
+
+## 5) Run tests
+
+Node payments:
+
+cd backend/payments
+npm test
+
+Dart rights and experiments:
+
+flutter pub get
+flutter analyze backend/rights backend/experiments test/backend/rights test/experiments
+flutter test test/backend/rights
+flutter test test/experiments/assignment_test.dart
+
+## 6) Payment flow smoke
+
+- Create checkout session via frontend or API.
+- Complete payment in provider test mode.
+- Verify rows:
+
+SQL:
+
+SELECT * FROM orders WHERE checkout_session_id = 'cs_test_abc123';
+SELECT * FROM ppv_entitlements WHERE order_id = '<orderId>';
+SELECT * FROM ledger_entries WHERE order_id = '<orderId>';
+
+## 7) Replay and reconciliation
+
+Dry run replay:
+
+node scripts/replay_webhooks.js --dry-run --limit=20
+
+Small live replay:
+
+node scripts/replay_webhooks.js --limit=20
+
+Reconciliation:
+
+./scripts/run-reconciliation.sh --date $(date +%F)
+
+or
+
+psql "$env:PG_CONN" -c "\copy (SELECT provider_tx_id, amount, status FROM provider_transactions WHERE created_at >= CURRENT_DATE - INTERVAL '1 day') TO 'reconciliation_report.csv' CSV HEADER"
+
+## 8) Acceptance criteria
+
+- CI workflow .github/workflows/payments-ci.yml is green.
+- Migration up/down smoke passes.
+- Payments node tests pass.
+- Rights and experiments tests pass.
+- Reconciliation has zero critical mismatches or documented triage items.
+
+## 9) Rollout guidance
+
+1. Deploy behind feature flag (default OFF).
+2. Internal canary for 24 hours.
+3. Public cohort ramp: 1% -> 10% -> 50% -> 100%.
+4. Rollback path: feature flag OFF, run rollback migration if required, replay events.
diff --git a/migrations/20260702_create_payments_and_webhooks.sql b/migrations/20260702_create_payments_and_webhooks.sql
index 497584ab..0208beff 100644
--- a/migrations/20260702_create_payments_and_webhooks.sql
+++ b/migrations/20260702_create_payments_and_webhooks.sql
@@ -1,4 +1,4 @@
- YOU WORKINGH OR STUCK -- migrations/20260702_create_payments_and_webhooks.sql
+-- migrations/20260702_create_payments_and_webhooks.sql
 -- Create payments, webhooks, entitlements, ledger, provider transactions, and payout tables
 -- Run with: psql $PG_CONN -f migrations/20260702_create_payments_and_webhooks.sql
 -- Rollback: psql $PG_CONN -f migrations/20260702_drop_payments_and_webhooks.sql
diff --git a/scripts/test/bootstrap_pg.ps1 b/scripts/test/bootstrap_pg.ps1
new file mode 100644
index 00000000..c9d63a7d
--- /dev/null
+++ b/scripts/test/bootstrap_pg.ps1
@@ -0,0 +1,43 @@
+$ErrorActionPreference = 'Stop'
+
+$containerName = $env:PG_CONTAINER_NAME
+if ([string]::IsNullOrWhiteSpace($containerName)) { $containerName = 'dfc-pg-test' }
+
+$user = $env:POSTGRES_USER
+if ([string]::IsNullOrWhiteSpace($user)) { $user = 'dfc' }
+
+$pass = $env:POSTGRES_PASSWORD
+if ([string]::IsNullOrWhiteSpace($pass)) { $pass = 'dfc' }
+
+$db = $env:POSTGRES_DB
+if ([string]::IsNullOrWhiteSpace($db)) { $db = 'dfc_test' }
+
+$port = $env:POSTGRES_PORT
+if ([string]::IsNullOrWhiteSpace($port)) { $port = '5432' }
+
+$existing = docker ps -a --format "{{.Names}}" | Where-Object { $_ -eq $containerName }
+if ($existing) {
+  docker rm -f $containerName | Out-Null
+}
+
+docker run -d --name $containerName `
+  -e "POSTGRES_USER=$user" `
+  -e "POSTGRES_PASSWORD=$pass" `
+  -e "POSTGRES_DB=$db" `
+  -p "${port}:5432" `
+  postgres:15 | Out-Null
+
+$env:PG_CONN = "postgres://${user}:${pass}@localhost:${port}/${db}"
+Write-Host "PG_CONN=$($env:PG_CONN)"
+Write-Host "Waiting for PostgreSQL readiness..."
+
+for ($i = 0; $i -lt 60; $i++) {
+  docker exec $containerName pg_isready -U $user -d $db 2>$null | Out-Null
+  if ($LASTEXITCODE -eq 0) {
+    Write-Host "PostgreSQL is ready in container $containerName"
+    exit 0
+  }
+  Start-Sleep -Seconds 1
+}
+
+throw "PostgreSQL did not become ready in time"
diff --git a/scripts/test/teardown_pg.ps1 b/scripts/test/teardown_pg.ps1
new file mode 100644
index 00000000..7c250797
--- /dev/null
+++ b/scripts/test/teardown_pg.ps1
@@ -0,0 +1,13 @@
+$ErrorActionPreference = 'Stop'
+
+$containerName = $env:PG_CONTAINER_NAME
+if ([string]::IsNullOrWhiteSpace($containerName)) { $containerName = 'dfc-pg-test' }
+
+$existing = docker ps -a --format "{{.Names}}" | Where-Object { $_ -eq $containerName }
+if ($existing) {
+  docker rm -f $containerName | Out-Null
+  Write-Host "Removed $containerName"
+}
+else {
+  Write-Host "Container $containerName not found"
+}
